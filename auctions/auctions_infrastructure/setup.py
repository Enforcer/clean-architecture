from setuptools import (
    find_packages,
    setup,
)

setup(
    name='auctions_infrastructure',
    packages=find_packages(),
    install_requires=[],
    extra_requires=[],
    python_requires=">=3.6",
)
